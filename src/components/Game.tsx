import React from "react";
import Timer from "./Timer";

const Game: React.FC = () => {
  return (
    <div>
      <section>
        <Timer />
      </section>
    </div>
  );
};

export default Game;
